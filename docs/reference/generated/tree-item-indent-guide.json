{
  "name": "TreeItemIndentGuide",
  "description": "Renders vertical indent guide lines for a tree item.\nOne guide line (`<span>`) is rendered per ancestor depth level (1 to level-1).\nEach guide line receives `data-depth` and optionally `data-last` attributes\nfor CSS-driven positioning and VS Code-style last-child truncation.\nRenders a `<span>` element containing inner `<span>` elements.",
  "props": {
    "className": {
      "type": "string | ((state: Tree.ItemIndentGuide.State) => string | undefined)",
      "description": "CSS class applied to the element, or a function that\nreturns a class based on the component’s state.",
      "detailedType": "| string\n| ((\n    state: Tree.ItemIndentGuide.State,\n  ) => string | undefined)"
    },
    "style": {
      "type": "CSSProperties | ((state: Tree.ItemIndentGuide.State) => CSSProperties | undefined)",
      "detailedType": "| React.CSSProperties\n| ((\n    state: Tree.ItemIndentGuide.State,\n  ) => CSSProperties | undefined)\n| undefined"
    },
    "render": {
      "type": "ReactElement | ((props: HTMLProps, state: Tree.ItemIndentGuide.State) => ReactElement)",
      "description": "Allows you to replace the component’s HTML element\nwith a different tag, or compose it with another component.\n\nAccepts a `ReactElement` or a function that returns the element to render.",
      "detailedType": "| ReactElement\n| ((\n    props: HTMLProps,\n    state: Tree.ItemIndentGuide.State,\n  ) => ReactElement)"
    }
  },
  "dataAttributes": {
    "data-depth": {
      "description": "The depth level this guide line represents (1-based)."
    },
    "data-last": {
      "description": "Present when the ancestor at this guide's depth is the last child of its parent.\nThis allows CSS to truncate the guide line (VS Code-style)."
    }
  },
  "cssVariables": {
    "--depth": {
      "description": "The depth level this individual guide line represents (1-based).\nSet on each inner `<span>` within the indent guide wrapper.",
      "type": "number"
    }
  }
}

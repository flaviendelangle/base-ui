export enum ListboxItemCssVars {
  /**
   * The horizontal distance from the item's new position back to its previous position.
   * Present with `data-displacing`.
   * @type {number}
   */
  displacementX = '--drag-displacement-x',
  /**
   * The vertical distance from the item's new position back to its previous position.
   * Present with `data-displacing`.
   * @type {number}
   */
  displacementY = '--drag-displacement-y',
}

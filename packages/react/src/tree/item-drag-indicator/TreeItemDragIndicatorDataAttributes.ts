export enum TreeItemDragIndicatorDataAttributes {
  /**
   * Present when the item is draggable.
   */
  draggable = 'data-draggable',
  /**
   * Present when the item is currently being dragged.
   */
  dragging = 'data-dragging',
}

export enum TreeItemIndentGuideCssVars {
  /**
   * The depth level this individual guide line represents (1-based).
   * Set on each inner `<span>` within the indent guide wrapper.
   * @type {number}
   */
  depth = '--depth',
}
